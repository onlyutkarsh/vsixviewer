trigger:
  - master
  
pool:
    name: Azure Pipelines

stages:
  - template: templates/build.yml
    parameters:
      stageName: 'build'
      jobName: 'hosted'
      publish: false
      pool:
        vmImage: 'windows-latest'